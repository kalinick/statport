<table class="default">
    <thead>
    <tr>
        <th>Meet</th>
        <th>Date</th>
        <th>Event</th>
        <th>Time</th>
    </tr>
    </thead>
    <tbody>
    {% set i = 0 %}
    {% for event in report %}
        <tr class="{% if (i % 2 == 1) %}grayed{% endif %}">
            <td>{{ event.res.event.meet }}</td>
            <td>{{ event.res.event.meet.date.format('d/m/Y') }}</td>
            <td>{{ event.eventTitle }}</td>
            <td>{{ event.res.seconds|timeToString }}</td>
        </tr>
        {% set i = i + 1 %}
    {% endfor %}
    </tbody>
</table>